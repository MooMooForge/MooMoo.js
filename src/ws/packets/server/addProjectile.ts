import { MooMoo } from "../../../../app";

function addProjectile(data: any) {
    // idk

    MooMoo.emit("addProjectile", data);
    MooMoo.emit("addprojectile", data);
    MooMoo.emit("18", data);
}

export default addProjectile;