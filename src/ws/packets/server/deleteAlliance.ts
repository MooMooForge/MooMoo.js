import { MooMoo } from "../../../../app";

function deleteAlliance(data: any) {
    // idk

    MooMoo.emit("deleteAlliance", data);
    MooMoo.emit("deletealliance", data);
}

export default deleteAlliance;