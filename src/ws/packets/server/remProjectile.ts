import { MooMoo } from "../../../../app";

function remProjectile(data: any) {
    // idk

    MooMoo.emit("remProjectile", data);
    MooMoo.emit("remprojectile", data);
    MooMoo.emit("19", data);
}

export default remProjectile;