import { MooMoo } from "../../../../app";

function gatherAnimation(data:any) {
   // idk

    MooMoo.emit("gatherAnimation", data);
    MooMoo.emit("gatheranimation", data);
    MooMoo.emit("7", data)
}

export default gatherAnimation;