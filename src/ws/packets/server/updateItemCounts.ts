import { MooMoo } from "../../../../app";

function updateItemCounts(data: any) {
    // idk

    MooMoo.emit("updateItemCounts", data);
    MooMoo.emit("updateitemcounts", data);
    MooMoo.emit("14", data);
}

export default updateItemCounts;